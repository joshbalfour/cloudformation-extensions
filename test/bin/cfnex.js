const assert = require('chai').assert;

describe('Cloudformation Extensions.cfnex executable', function(){
	
	let cfnex;

	it('should init fine', function(){
		// cfnex = require('../../bin/cfnex');
	});

	it('should have the correct arguments', function(){
		
	});

	it('should run fine with correct arguments', function(){
		// cfnex();
	});

	it('should error property with no arguments', function(){
		// cfnex();
	});

	it('should put files in the right places when ran', function(){
		
	});

	it('should only output debug statements when the --debug flag is specified', function(){
		
	});

});