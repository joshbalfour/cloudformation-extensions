sudo: required

services:
  - docker

install:
  - docker build -t cfnex .
  - docker run --entrypoint /bin/sh cfnex 'cd /src && npm install'

script:
  - docker run --entrypoint /bin/sh cfnex 'cd /src && npm test'